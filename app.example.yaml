runtime: nodejs12
service: service-name

instance_class: F2

automatic_scaling:
  max_instances: 1
  min_instances: 0

env_variables:
  NODE_ENV: development

  DB_CONNSTRING: /cloudsql/<instance-name>
  DB_USERNAME: username
  DB_PASSWORD: password
  DB_NAME: dbname
